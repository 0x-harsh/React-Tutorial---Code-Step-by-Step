- BrowserRouter: This component enables client-side routing using the browser's history API.

- Routes: It's responsibile for rendering the appropriate component based on the current URL.

- Route: Each Route component defines a path and the corresponding component to render when the path is matched.

- Link: A link for navigation from 1 page to other page.